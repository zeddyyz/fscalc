import 'package:flutter/material.dart';

class ForexFixedScreen extends StatefulWidget {
  const ForexFixedScreen({Key? key}) : super(key: key);

  @override
  _ForexFixedScreenState createState() => _ForexFixedScreenState();
}

class _ForexFixedScreenState extends State<ForexFixedScreen> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("HELLO"),
    );
  }
}
